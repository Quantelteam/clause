{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd backend && python -m uvicorn clauseiq.api:app --host 0.0.0.0 --port $PORT",
    "restartPolicyMaxRetries": 5
  }
}
