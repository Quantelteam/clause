services:
  - type: web
    name: clauseiq
    env: python
    root: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn clauseiq.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
